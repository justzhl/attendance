<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">
<struts>
	<constant name="struts.devMode" value="true"></constant>
	<constant name="struts.objectFactory" value="spring"></constant>
    <!-- struts2的action必须放在一个指定的包空间下定义 -->
    <package name="default" extends="struts-default" namespace="/">
        <!-- 定义处理请求URL为login.action的Action -->
        <action name="login" class="LoginAction">
            <!-- 定义处理结果字符串和资源之间的映射关系 -->
            <result name="success">main.jsp</result>
            <result name="error">error.jsp</result>
        </action>
        <action name="liststudentinterval" class="AttendanceAction">
        	<result>liststudentattendance.jsp</result>
        </action>
    </package>
    
    <package name="teacher" extends="struts-default" namespace="/teacher">
        <action name="TeachMessage" class="TeachMessage" method="sendMessage">
        	<result name="success">/teacher/messages1.jsp</result>
        	<result name="input">/teacher/messages.jsp</result>
        	<result name="error">/ajax/index.html</result>
        </action>
        <action name="GetMyInstructorId" class="TeachMessage" method="getMyInstructorId">
        </action>
    </package>
    
    
    <package name="json" extends="json-default" namespace="/json">
        <!-- 定义处理请求URL为login.action的Action -->
        <action name="liststudents" class="listStudents">
            <!-- 定义处理结果字符串和资源之间的映射关系 -->
            <result type="json">
            	<param name="root">datamap</param>
            </result>
        </action>
        <action name="listpickedcourses" class="listpickedcoursesAction">
            <result name="success" type="json">
            	<param name="root">dataSet</param>
            </result>
            <result name="login">/login.html</result>
            <result name="error">/404.html</result>
        </action>
        <action name="GetTeachMessage" class="GetTeachMessageAction">
            <result name="success" type="json">
            	<param name="root">dataMap</param> 
            </result>
            <result name="login">/login.html</result>
            <result name="error">/404.html</result>
        </action>
        
        <action name="GetMyAttendance" class="GetMyAttendance">
            <result name="success" type="json">
            	<param name="root">dataMap</param> 
            </result>
            <result name="login">/login.html</result>
            <result name="error">/404.html</result>
        </action>
    </package>
</struts>